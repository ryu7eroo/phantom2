[project]
name = "ctf-swarm"
version = "0.1.0"
description = "Multi-agent CTF solving swarm"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8,<9"]

[tool.pytest.ini_options]
pythonpath = ["."]
